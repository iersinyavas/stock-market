package com.artsoft.stock.configuration;

/*
import io.swagger.v3.oas.models.Components;
import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Contact;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.security.SecurityRequirement;
import io.swagger.v3.oas.models.security.SecurityScheme;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {

    @Bean
    public OpenAPI openAPI(){
        return new OpenAPI().info(new Info()
                .title("Stock Market API")
                .description("Stock Market API Documentation")
                .contact(new Contact().name("İbrahim Ersin YAVAŞ").email("iersinyavas@gmail.com"))
                .version("1.0.0")
        );
    }

 */
/*       @Bean
        public OpenAPI openAPI(){
        return new OpenAPI().addSecurityItem(new SecurityRequirement()
                        .addList("Bearer Authentication"))
                .components(new Components().addSecuritySchemes("Bearer Authentication", this.createAPIKeyScheme()))
                .info(new Info()
                .title("Stock Market API")
                .description("Stock Market API Documentation")
                .contact(new Contact().name("İbrahim Ersin YAVAŞ").email("iersinyavas@gmail.com"))
                .version("1.0.0")
        );
    }

    private SecurityScheme createAPIKeyScheme(){
        return new SecurityScheme()
                .type(SecurityScheme.Type.HTTP)
                .bearerFormat("JWT")
                .scheme("bearer");
    }*//*

}
*/
